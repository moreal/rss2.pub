nodeLinker: pnpm

# 0.6.0-dev.348 was requested explicitly (native federationOptions
# passthrough, fedify-dev/botkit#41); exact descriptors so the minimal-age
# gate still covers everything else.
npmPreapprovedPackages:
  - "@fedify/botkit@0.6.0-dev.348"
  - "@fedify/botkit-postgres@0.6.0-dev.348"
