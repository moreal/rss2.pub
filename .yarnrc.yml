nodeLinker: pnpm

# 0.6.0-dev.345 was requested explicitly (Create-then-Update fix); exact
# descriptors so the minimal-age gate still covers everything else.
npmPreapprovedPackages:
  - "@fedify/botkit@0.6.0-dev.345"
  - "@fedify/botkit-postgres@0.6.0-dev.345"
